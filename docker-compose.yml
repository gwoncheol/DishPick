services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dishpick-app
    restart: unless-stopped
    environment:
      DATABASE_PATH: /app/data/dishpick.db
    ports:
      - "80:3000"
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
